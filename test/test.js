var gulp   = require('gulp'),
    inject = require('../src/index');

(function () {
    // gulp.src('./test/**/*.html')
    //     .pipe(inject.server({
    //         script: '1111'
    //     }))
    //     .pipe(gulp.dest('./test'));

    // gulp.src('./test/**/*.html')
    //     .pipe(inject.dist())
    //     .pipe(gulp.dest('./test'));
})();
